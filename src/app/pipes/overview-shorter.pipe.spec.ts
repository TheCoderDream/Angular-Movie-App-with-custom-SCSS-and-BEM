import { OverviewShorterPipe } from './overview-shorter.pipe';

describe('OverviewShorterPipe', () => {
  it('create an instance', () => {
    const pipe = new OverviewShorterPipe();
    expect(pipe).toBeTruthy();
  });
});
