import { CovertMoneyPipe } from './covert-money.pipe';

describe('CovertMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new CovertMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
